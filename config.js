// Global configuration
const SplConfig = {
  // Switch to false for production
  useTestData: true,
  
  // API endpoints (for real data)
  apiUrl: 'https://api.splitra.com',
  
  // Enable/disable console logging
  enableLogging: true,
  
  // Default currency
  defaultCurrency: 'ETH',
  
  // Page paths
  pages: {
    dashboard: '/dashboard',
    contracts: '/contracts',
    activities: '/activities',
    profile: '/profile'
  }
};
